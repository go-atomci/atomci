name: Compile

on:
  push:
    branches:
      - contributing-help

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - name: Buile atomci backend
        run: make build
#       - name: Build frontend page
#         uses: actions/setup-node@v2
#         with:
#           node-version: '12'
#       - run: |
#           cd web
#           yarn install
#           yarn run build
      
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Build atomci frontend
        uses: docker/build-push-action@v2
        with:
          context: ./
          file: ./Dockerfile
          builder: ${{ steps.buildx.outputs.name }}
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/atomci:test
          cache-from: type=registry,ref=${{ secrets.DOCKER_HUB_USERNAME }}/atomci-frontend:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKER_HUB_USERNAME }}/atomci-frontend:buildcache,mode=max
      - name: Change to root dir
        run: | 
          cd ../
          pwd
      - name: Build atomci backend
        uses: docker/build-push-action@v2
        with:
          context: ./
          file: ./Dockerfile
          builder: ${{ steps.buildx.outputs.name }}
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/atomci:test
          cache-from: type=registry,ref=${{ secrets.DOCKER_HUB_USERNAME }}/atomci:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKER_HUB_USERNAME }}/atomci-frontend:buildcache,mode=max
     
